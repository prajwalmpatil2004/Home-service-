name: yhome
description: A HOME SERVICE APP.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.5.1 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.20.2
  flutter_rating_bar: ^4.0.0
  provider: ^6.0.0
  google_fonts: ^6.2.1
  cupertino_icons: ^1.0.8
  animate_do: ^4.2.0
  scrollable_positioned_list: ^0.3.8

  # ✅ Firebase dependencies (aligned versions)
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.0
  cloud_firestore: ^5.4.4
  firebase_storage: ^12.3.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
